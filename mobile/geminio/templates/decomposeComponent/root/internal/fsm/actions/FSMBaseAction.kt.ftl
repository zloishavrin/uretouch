package ${packageName}.internal.fsm.actions

import ${packageName}.internal.fsm.state.${FSMState}
import ru.kontur.mobile.visualfsm.Action

internal sealed class ${FSMBaseAction} : Action<${FSMState}>()