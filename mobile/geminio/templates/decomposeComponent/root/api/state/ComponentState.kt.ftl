package ${packageName}.api.state

class ${componentState}(

) {
    companion object {
        fun initial(): ${componentState} {
            return ${componentState}(

            )
        }
    }
}